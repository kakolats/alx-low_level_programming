#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	for (int numb = 0 ; numb < 10 ; numb++)
		printf("%d", numb);
	putchar('\n');  
	return (0);
}
