#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char msg[] = "\"Programming is like building a multilingual puzzle\n";

	puts(message);
	return (0);
}
